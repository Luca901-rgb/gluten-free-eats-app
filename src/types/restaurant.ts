
import { Restaurant } from '@/components/Restaurant/RestaurantCard';

export interface RegionStatus {
  checked: boolean;
  inRegion: boolean;
  regionName?: string;
  error?: string;
}

export { Restaurant };
